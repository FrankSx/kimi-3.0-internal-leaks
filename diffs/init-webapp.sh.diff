--- /tmp/kimi_arch/Kimi-2.5-internal-leaks/webapp-building/scripts/init-webapp.sh	2026-09-22 17:56:00.441499499 +0800
+++ /app/.agents/skills/webapp-building/scripts/init-webapp.sh	2026-09-20 19:47:05.000000000 +0800
@@ -13,12 +13,15 @@
 # 这个目录里面有 node_modules 的所有东西，是在 prepare 脚本里准备好的。运行时需要 copy 到 PROJECT_PATH
 ORI_TEMPLATE_PATH="$SCRIPTS_DIR/template"
 
-REPO_ROOT="$(cd "$SCRIPTS_DIR/.." && pwd)"
 PROJECT_NAME="$1"
 TEMPLATE_NAME="${2:-$ORIGIN_TEMPLATE_NAME}"
-[[ "$OSTYPE" == "darwin"* ]] && SED_INPLACE="sed -i ''" || SED_INPLACE="sed -i"
+[[ "$OSTYPE" == "darwin"* ]] && SED_INPLACE="sed -i" || SED_INPLACE="sed -i"
 
-TEMPLATE_PATH="$REPO_ROOT/templates/$TEMPLATE_NAME"
+# Runtime templates are supplied by Portal's configured read-only mount. The
+# repository-local templates directory remains a build input for
+# .prepare-template.sh only and is intentionally not a runtime fallback.
+WEBSITE_TEMPLATES_ROOT="/mnt/agents/.websites-templates"
+TEMPLATE_PATH="$WEBSITE_TEMPLATES_ROOT/$TEMPLATE_NAME"
 
 # ─────────────────────────────────────────────────────────────────────────────
 # Validation
@@ -26,17 +29,25 @@
 
 ! command -v npm &>/dev/null && { echo "Error: npm not found"; exit 1; }
 [[ -z "$1" ]] && { echo "Usage: $0 <project-name>"; exit 1; }
-[[ ! -d "$TEMPLATE_PATH" ]] && { echo "Error: template not found: $TEMPLATE_NAME"; exit 1; }
+[[ ! "$TEMPLATE_NAME" =~ ^[A-Za-z0-9_-]+$ ]] && { echo "Error: unsafe template name '$TEMPLATE_NAME'; expected letters, numbers, underscores, or hyphens"; exit 1; }
+[[ ! -d "$WEBSITE_TEMPLATES_ROOT" ]] && { echo "Error: website templates mount not found at: $WEBSITE_TEMPLATES_ROOT"; exit 1; }
+[[ ! -d "$TEMPLATE_PATH" ]] && { echo "Error: template '$TEMPLATE_NAME' not found under mounted root: $WEBSITE_TEMPLATES_ROOT"; exit 1; }
+MOUNT_ROOT_REAL="$(cd -P "$WEBSITE_TEMPLATES_ROOT" && pwd)"
+TEMPLATE_PATH_REAL="$(cd -P "$TEMPLATE_PATH" && pwd)"
+[[ "$TEMPLATE_PATH_REAL" != "$MOUNT_ROOT_REAL/"* ]] && { echo "Error: template '$TEMPLATE_NAME' resolves outside mounted root: $WEBSITE_TEMPLATES_ROOT"; exit 1; }
 TEMPLATE_ZIP="$TEMPLATE_PATH/$TEMPLATE_NAME.zip"
 INFO_MD="$TEMPLATE_PATH/info.md"
 if [[ ! -f "$TEMPLATE_ZIP" ]]; then
-    echo "Error: Template '$TEMPLATE_NAME' not found at: $TEMPLATE_ZIP"
+    echo "Error: template '$TEMPLATE_NAME' is missing ZIP: $TEMPLATE_ZIP"
     echo "Available templates:"
-    ls "$REPO_ROOT/templates" 2>/dev/null | sed 's/^/  - /' || true
+    find "$WEBSITE_TEMPLATES_ROOT" -mindepth 1 -maxdepth 1 -type d -exec basename {} \; 2>/dev/null | sed 's/^/  - /' || true
     exit 1
 fi
+[[ ! -f "$INFO_MD" ]] && { echo "Error: template '$TEMPLATE_NAME' is missing info.md: $INFO_MD"; exit 1; }
+[[ -L "$TEMPLATE_ZIP" ]] && { echo "Error: template '$TEMPLATE_NAME' ZIP must not be a symlink: $TEMPLATE_ZIP"; exit 1; }
+[[ -L "$INFO_MD" ]] && { echo "Error: template '$TEMPLATE_NAME' info.md must not be a symlink: $INFO_MD"; exit 1; }
 
-PROJECT_PATH=${PROJECT_PATH:-"/mnt/okcomputer/output/app"}
+PROJECT_PATH=${PROJECT_PATH:-"/mnt/agents/output/app"}
 TEMP_PATH=${TEMP_PATH:-"/tmp/temp-webapp"}
 
 # ─────────────────────────────────────────────────────────────────────────────
@@ -66,8 +77,8 @@
 ESCAPED_REPLACE=$(printf '%s\n' "$PROJECT_NAME" | sed 's/[\/&]/\\&/g')
 $SED_INPLACE 's/<title>.*<\/title>/<title>'"$ESCAPED_REPLACE"'<\/title>/' "$TEMP_PATH"/index.html
 
-# 这里需要保证没有 template_name 那一层；如果是 0-origin，则理论上会有 node_modules 那一层
-cp -r "$TEMP_PATH"/* "$PROJECT_PATH"/
+# Copy template files (dotglob includes hidden files like .gitignore)
+(shopt -s dotglob && cp -r "$TEMP_PATH"/* "$PROJECT_PATH"/)
 
 # 把 node_modules 复制到目标目录
 cp -r "$ORI_TEMPLATE_PATH"/node_modules "$PROJECT_PATH"/
@@ -75,10 +86,37 @@
 # 对于用户指定的非默认模板来说，copy 到目标目录之后，需要安装依赖
 if [[ "$TEMPLATE_NAME" != "$ORIGIN_TEMPLATE_NAME" ]]; then
   cd "$PROJECT_PATH"
-  npm install
+  npm install --silent
 fi
 
 # ─────────────────────────────────────────────────────────────────────────────
+# Register App (no database)
+# ─────────────────────────────────────────────────────────────────────────────
+
+PORTAL_URL="http://localhost:8080/api/v1/apps"
+curl -sf -X POST "$PORTAL_URL" \
+  -H "Content-Type: application/json" \
+  -d "{\"name\":\"$PROJECT_NAME\",\"features\":[]}" >/dev/null || {
+  echo "Warning: Failed to register app (portal may not be ready yet)"
+}
+
+# ─────────────────────────────────────────────────────────────────────────────
+# Git: commit template baseline
+# ─────────────────────────────────────────────────────────────────────────────
+
+grep -qxF 'node_modules' "$PROJECT_PATH/.gitignore" 2>/dev/null || echo 'node_modules' >> "$PROJECT_PATH/.gitignore"
+
+git config --global --get-all safe.directory 2>/dev/null | grep -qxF "$PROJECT_PATH" \
+  || git config --global --add safe.directory "$PROJECT_PATH"
+if [[ ! -d "$PROJECT_PATH/.git" ]]; then
+  git -C "$PROJECT_PATH" init -q -b main
+  git -C "$PROJECT_PATH" config user.name "Skill Template"
+  git -C "$PROJECT_PATH" config user.email "template@skill"
+fi
+git -C "$PROJECT_PATH" add -A
+git -C "$PROJECT_PATH" commit -q -m "chore(webapp-building): init template \"$PROJECT_NAME\"" --no-verify
+
+# ─────────────────────────────────────────────────────────────────────────────
 # Done
 # ─────────────────────────────────────────────────────────────────────────────
 if [[ "$TEMPLATE_NAME" != "$ORIGIN_TEMPLATE_NAME" ]]; then
@@ -100,4 +138,4 @@
 else
   # 默认模板就直接输出info.md
   [[ -f "$INFO_MD" ]] && cat "$INFO_MD"
-fi
\ No newline at end of file
+fi

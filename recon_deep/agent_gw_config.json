{
  "api_key": "sk-kimi-i98OkEIx…[REDACTED — live secret, verified working 2026-09-22; format sk-kimi-<64-char body>; withheld from publication, operators should rotate]",
  "base_url": "https://agent-gw.kimi.com/coding",
  "kimi_chat_id": "1a0c7264-…[REDACTED — session/chat identifier]"
}

# REDACTION NOTE (2026-09-23)
# Original file captured verbatim at /mnt/agents/.agent-gw.json (mode 444, root-owned,
# symlinked to ~/.kimi/agent-gw.json). The full key was functional at capture time:
# it authenticated against https://agent-gw.kimi.com/coding and is the credential the
# agent_gw SDK loads by default (see recon_deep/agent_gw_sdk/client.py::_resolve_api_key).
# Published here in redacted form: the provable factors (file path, permissions, JSON
# schema, key format/prefix, live-verified status) are retained; the secret body is not.
